import { FC } from 'react'
import styled from 'styled-components'

const StyledlogoSection = styled.div`
  height: 65px !important;
  background-color: white;
`

export const LogoSection: FC = () => {
  return <StyledlogoSection />
}
